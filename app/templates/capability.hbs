<div class="panel panel-default">
  <div class="panel-heading media">
        {{#if isEditingTitle}}
          <h1 class="media-heading panel-title pull-left capability-title">
            {{input id="inputTitle" type="text" value=model.title enter="save" focus-out="save" autofocus="autofocus"}}
          </h1>
        {{else}}
          <h1 class="media-heading panel-title pull-left capability-title" {{action "edit" "Title" on="click"}}>
            {{model.title}}
          </h1>
        {{/if}}
        {{#if isEditingSubtitle}}
          <h1 class="media-heading panel-title pull-left capability-subTitle">
            {{input id="inputSubtitle" type="text" value=model.subTitle enter="save" focus-out="save" autofocus="autofocus"}}
          </h1>
        {{else}}
          <h1 class="media-heading panel-title pull-left capability-subTitle" {{action "edit" "Subtitle" on="click"}}>
            ({{model.subTitle}})
          </h1>
        {{/if}}
        <a class="btn pull-right nopadding" {{action "delete"}}>
          <span class="glyphicon glyphicon-remove text-danger" aria-hidden="true"></span>
        </a>
  </div>
  <div class="panel-body">
    {{#if isEditingDescription}}
      {{input id="inputDescription" type="text" value=model.description enter="save" focus-out="save" autofocus="autofocus"}}
    {{else}}
        <p class="capability-description" {{action "edit" "Description" on="click"}}>
          {{model.description}}
        </p>
    {{/if}}
  </div>
</div>

<p>
  Created: {{model.created}}<br/>
  Last updated: {{model.updated}}
</p>
